from main import *
import pygame
from gfx.loadSprite import Sprites

def BulletShower(x, amount, spread, sprite):
	for n in range(amount):
		spawnbullet(x+n*spread,0,0,sprite)


def BulletFan(x, amount, spread, delay, sprite):
	

def StraightDown(x,y, Tracer, sprite):
		

def Function(funcx, funcy, x, y, sprite):


def Spawnbullet(x,y,AP,angle,sprite)
	bullet = Sprites(x,y,loaded_Sprites, 0, sprite)


